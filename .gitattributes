# Shell scripts must keep LF on checkout. Windows clones default to core.autocrlf=true, and a script
# checked out with CRLF fails at its own shebang (`/usr/bin/env bash\r: no such file or directory`).
*.sh text eol=lf
